# 🔄 Руководство по перезапуску фронтенда

## Проблема, которую мы решили

**Симптом:** Страницы показывали "Loading..." навсегда
**Причина:** Старый кэш Next.js с ошибками компиляции
**Ошибка в консоли:** `Uncaught SyntaxError: Invalid or unexpected token` в layout.js:91

## ✅ Решение: Полный перезапуск с очисткой кэша

### Шаг 1: Остановить все процессы на порту 3001

Откройте PowerShell или CMD **от имени администратора** и выполните:

```powershell
# Найти процесс на порту 3001
netstat -ano | findstr :3001

# Вывод будет примерно таким:
# TCP    0.0.0.0:3001           0.0.0.0:0              LISTENING       2948
#         ↑ это PID процесса

# Убить процесс (замените 2948 на ваш PID)
taskkill /F /PID 2948
```

**Альтернатива (если не работает):**
```powershell
# Убить все процессы node.exe
taskkill /F /IM node.exe
```

### Шаг 2: Очистить кэш Next.js

В PowerShell или CMD:

```powershell
cd C:\whatsapp-saas-starter\Frontend

# Удалить папку .next (кэш компиляции)
rmdir /s /q .next

# Удалить node_modules/.cache (опционально, если проблемы не ушли)
rmdir /s /q node_modules\.cache
```

### Шаг 3: Запустить фронтенд заново

```powershell
cd C:\whatsapp-saas-starter\Frontend

npm run dev
```

**Ожидаемый вывод:**
```
▲ Next.js 14.2.33
- Local:        http://localhost:3001
✓ Starting...
✓ Ready in 2-5s
```

### Шаг 4: Проверить в браузере

Откройте: http://localhost:3001

**Ожидаемый результат:**
- Landing page загружается мгновенно
- НЕТ "Loading..." экрана
- Видны все секции страницы

---

## 🔧 Если всё ещё не работает

### Проверка 1: Очистить кэш браузера

1. Откройте DevTools (F12)
2. Зайдите в **Application** → **Storage**
3. Нажмите **Clear site data**
4. Перезагрузите страницу (Ctrl+Shift+R)

### Проверка 2: Очистить localStorage

В консоли браузера (F12 → Console):
```javascript
localStorage.clear()
sessionStorage.clear()
location.reload()
```

### Проверка 3: Полная переустановка зависимостей

```powershell
cd C:\whatsapp-saas-starter\Frontend

# Удалить node_modules
rmdir /s /q node_modules

# Удалить package-lock.json
del package-lock.json

# Переустановить
npm install

# Запустить
npm run dev
```

### Проверка 4: Проверить ошибки в консоли

1. Откройте DevTools (F12)
2. Зайдите в **Console**
3. Найдите красные ошибки
4. Скопируйте текст ошибки

Типичные ошибки:

| Ошибка | Решение |
|--------|---------|
| `SyntaxError: Invalid or unexpected token` | Очистить кэш .next |
| `Failed to fetch` | Проверить, запущен ли бекенд на порту 3000 |
| `Cannot read property 'id' of undefined` | Выйти из аккаунта и войти снова |
| `EADDRINUSE: port 3001 already in use` | Убить процесс на порту 3001 (Шаг 1) |

---

## 📋 Быстрая команда (всё в одном)

Скопируйте и вставьте в PowerShell:

```powershell
# Остановить фронтенд
taskkill /F /IM node.exe 2>nul

# Перейти в папку
cd C:\whatsapp-saas-starter\Frontend

# Очистить кэш
rmdir /s /q .next 2>nul

# Запустить
npm run dev
```

---

## 🚀 Что было исправлено в коде

### 1. Исправлен баг в `auth.store.ts`

**Файл:** `Frontend\src\stores\auth.store.ts`
**Строки:** 383-394

**Проблема:** Функция `onRehydrateStorage` была неправильно реализована, из-за чего `isHydrated` оставался `false` навсегда.

**Было:**
```typescript
onRehydrateStorage: () => (state) => {
  state?.setHydrated(); // ❌ state = undefined когда localStorage пустой
},
```

**Стало:**
```typescript
onRehydrateStorage: (state) => {
  return (rehydratedState, error) => {
    if (error) {
      console.error('Failed to rehydrate auth state:', error);
    }
    // ✅ Всегда вызывает setHydrated()
    state.setHydrated();
  };
},
```

### 2. Заменён MOCK_SALON_ID на реальные данные

Во всех 14 файлах dashboard заменено:
```typescript
// Было:
const MOCK_SALON_ID = 'salon-123';

// Стало:
import { useSalonId } from '@/hooks/useSalonId';
const salonId = useSalonId();
```

### 3. Удалена неиспользуемая landing page

Удалена директория `landing-page/` со статическим HTML.

---

## ✅ Проверка что всё работает

После запуска фронтенда проверьте:

### 1. Landing Page (/)
```
http://localhost:3001
```
- ✅ Загружается мгновенно
- ✅ Видны все секции (Hero, Features, Pricing)

### 2. Registration (/register)
```
http://localhost:3001/register
```
- ✅ Форма регистрации появляется сразу
- ✅ НЕТ "Loading..."

### 3. Login (/login)
```
http://localhost:3001/login
```
- ✅ Форма входа появляется сразу
- ✅ НЕТ "Loading..."

### 4. Dashboard (требует авторизации)
```
http://localhost:3001/dashboard
```
- Без авторизации: ✅ Редирект на /login (правильное поведение)
- С авторизацией: ✅ Загружается dashboard

---

## 🐛 Логи для отладки

Если нужна помощь, приложите эти логи:

### Лог 1: Консоль браузера (DevTools → Console)
```
Скриншот или текст всех ошибок (красный текст)
```

### Лог 2: Консоль терминала
```
Последние 20 строк после npm run dev
```

### Лог 3: Network tab
```
DevTools → Network → All
Скриншот запросов с красным статусом (404, 500)
```

---

## 📞 Быстрая диагностика

| Что проверить | Команда | Ожидаемый результат |
|---------------|---------|---------------------|
| Фронтенд запущен? | `netstat -ano \| findstr :3001` | Должен показать процесс |
| Бекенд запущен? | `netstat -ano \| findstr :3000` | Должен показать процесс |
| .next папка очищена? | `dir .next` | Ошибка "не найден" или нет файлов |
| node_modules есть? | `dir node_modules` | Папка существует с файлами |

---

## ✨ После успешного запуска

Если всё работает, протестируйте полный флоу:

1. **Регистрация:** http://localhost:3001/register
   - Создайте нового пользователя
   - Должен перенаправить на /onboarding

2. **Onboarding:** Пройдите все 4 шага
   - Создайте салон
   - Добавьте сервис
   - Пропустите WhatsApp
   - Перейдите в dashboard

3. **Dashboard:** Проверьте все страницы
   - /dashboard/bookings
   - /dashboard/services
   - /dashboard/staff
   - /dashboard/profile
   - /dashboard/salon

4. **Logout → Login:** Выйдите и войдите снова
   - Должен сразу перейти в /dashboard

---

## 📝 Сводка команд

**Остановить:**
```powershell
taskkill /F /IM node.exe
```

**Очистить:**
```powershell
cd C:\whatsapp-saas-starter\Frontend
rmdir /s /q .next
```

**Запустить:**
```powershell
npm run dev
```

**Проверить:**
```powershell
http://localhost:3001
```

---

**Статус:** ✅ Фронтенд запущен и работает корректно!
**Порт:** 3001
**URL:** http://localhost:3001
