# 🚀 Быстрый Старт - 5 минут

## Вариант 1: Автоматические Тесты (Рекомендуется)

### Windows
```bash
cd Backend
test-integration.bat
```

### Mac/Linux
```bash
cd Backend
chmod +x test-integration.sh
./test-integration.sh
```

**Что произойдет:**
- ✅ Запустится PostgreSQL
- ✅ Применятся миграции
- ✅ Выполнятся 17 интеграционных тестов
- ✅ Вы увидите результаты в консоли

---

## Вариант 2: Локальный Сервер

### Windows
```bash
cd Backend

# 1. Создать .env файл (первый раз)
copy .env.example .env

# 2. Отредактировать .env и добавить:
#    OPENAI_API_KEY=sk-your-key-here
#    (остальное можно оставить как есть)

# 3. Запустить сервер
start-local-test.bat
```

### Mac/Linux
```bash
cd Backend

# 1. Создать .env файл (первый раз)
cp .env.example .env

# 2. Отредактировать .env
nano .env
# Добавить OPENAI_API_KEY=sk-your-key-here

# 3. Запустить сервер
chmod +x start-local-test.sh
./start-local-test.sh
```

**Сервер запустится на:** `http://localhost:3000`

**Проверить работу:**
```bash
curl http://localhost:3000/api/v1/health
```

---

## Вариант 3: С Настоящим WhatsApp

### Шаг 1: Запустить сервер (как выше)

### Шаг 2: Открыть туннель
```bash
# В новом терминале
ngrok http 3000
```

Копируем URL, например: `https://abc123.ngrok.io`

### Шаг 3: Настроить WhatsApp

1. Открыть: https://developers.facebook.com/
2. Ваше приложение → WhatsApp → Configuration
3. Webhook:
   - URL: `https://abc123.ngrok.io/api/v1/whatsapp/webhook`
   - Verify Token: из вашего `.env` файла
4. Subscriptions: включить `messages`

### Шаг 4: Отправить сообщение

Отправьте на ваш WhatsApp Business номер:
```
Стрижка в пятницу в 15:00
```

**Ожидаемый результат:**
1. Бот отправит карточку с доступными слотами
2. Вы нажмете на слот
3. Бот отправит подтверждение
4. Вы нажмете [Подтвердить]
5. Бот создаст бронирование ✅

---

## 📊 Просмотр Данных

### Prisma Studio (веб-интерфейс для БД)
```bash
npx prisma studio
```
Откроется: `http://localhost:5555`

### Логи в реальном времени
Логи выводятся в консоли где запущен сервер

### Аналитика
```bash
curl http://localhost:3000/api/ai/analytics/us1/success-criteria
```

---

## 🐛 Если что-то не работает

### Проблема: "Cannot connect to database"
```bash
docker ps | grep postgres
# Если не запущен:
docker-compose up -d postgres
```

### Проблема: "Port 3000 already in use"
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID [PID] /F

# Mac/Linux
lsof -ti:3000 | xargs kill -9
```

### Проблема: "OpenAI API error"
```bash
# Проверить .env файл
cat .env | grep OPENAI_API_KEY

# Убедиться что ключ начинается с sk-
# Получить новый: https://platform.openai.com/api-keys
```

---

## 📚 Подробная Документация

Полное руководство: `Backend/LOCAL_TESTING_GUIDE.md`

---

## ✅ Готово!

Теперь вы можете:
- ✅ Запустить интеграционные тесты
- ✅ Запустить локальный сервер
- ✅ Протестировать с настоящим WhatsApp
- ✅ Просмотреть данные в БД
- ✅ Отладить код в VS Code

**Время на запуск:** 5 минут
**Требуется:** Docker, Node.js 18+, PostgreSQL (через Docker)
